# Archivos de datos de CodeGraph — locales para cada máquina, no para hacer commit.
# Ignorar todo en .codegraph/ excepto este mismo archivo, para que los archivos
# transitorios (la base de datos, daemon.pid, sockets, logs) nunca aparezcan en git.
*
!.gitignore
